fun calculatePoints(wins: Int, draws: Int, losses: Int): Int {
    val wins = 5
    val draws = 2
    val losses = 3

    val totalPoints = calculatePoints(wins, draws, losses)

    println("Всего очков: $totalPoints")
}
